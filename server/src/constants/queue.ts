export enum QUEUEN_AME {
  JOBS = 'jobs-queue',
  LOGS = 'logs-queue',
}

export enum JOB_NAME {
  IMPORT = 'import-job',
  SYNC = 'sync-job',
  LOG_CLEANUP = 'log-cleanup',
  LOG = 'log-job',
}

export enum CRON_NAME {
  IMPORT_TRIGGER = 'import-trigger-cron',
}