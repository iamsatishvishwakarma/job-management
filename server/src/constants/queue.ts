export enum QUEUEN_AME {
  JOBS_FEED = "jobs-feed-queue",
  LOGS = "logs-queue",
}

export enum JOB_NAME {
  IMPORT = "import-job",
  SYNC = "sync-job",
  LOG_CLEANUP = "log-cleanup",
  LOG = "log-job",
}

export enum CRON_NAME {
  HOURLY_TRIGGER = "hourly-trigger-cron",
}
