NEXT_PUBLIC_API_URL=           # Base URL for your backend API
NEXT_PUBLIC_SOCKET_URL=        # WebSocket server URL (e.g., for real-time features)
NEXT_PUBLIC_NODE_ENV=          # Current environment (development | production)
